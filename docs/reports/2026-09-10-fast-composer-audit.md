# 2026-09-10 · Fast 输入栏与 Ultra 确认调查

## 证据与边界

- 正确源码仓库为 `Alunixa-Code/Alunixa-X`，本地路径为 `D:\Cursor\AlunixaX`，本轮没有修改已归档旧仓库喵~
- 只读安装资源为 `OpenAI.Codex 26.901.6511.0`，分析 ASAR 索引和 JavaScript AST，不执行安装包代码或连接当前桌面/CDP 喵~
- 本次官方网页查询没有返回可核验正文，因此根因依据是安装资源和隔离回归，而不是未读取成功的网页喵~

## Fast 根因与最小修复

| 观察项 | 当前安装包证据 | 修复 |
| --- | --- | --- |
| 共享 footer | `app-initial-f87238153a19.js` 的共享组件使用 `_ComposerFooter_*`，输出 `data-composer-footer-responsive` | 同时支持语义属性和 CSS 模块前缀 |
| 新版输入栏 footer | `app-primary-428a0a65766f.js` 的 footer 输出 `ComposerLayoutFooter` CSS 类及布局/间距/响应标记 | 不再依赖旧完整 utility 类列表；支持响应属性不存在的分支 |
| 刷新入口 | 扩展的扫描选择器原先仅包含 `.composer-footer` | 新旧选择器统一参与现有有界扫描 |
| 插入位置 | 旧逻辑可能选中大范围祖先、原生按钮或控件自身 | footer 优先、交互祖先排除、跳过既有 badge |

实际 CSS 类分别为 `_ComposerFooter_q5yh8_1` 和 `_ComposerLayoutFooter_kbwao_2`，两者均由新增前缀选择器覆盖喵~

资源文件名与完整类名只记录审计证据，不在产品中硬编码；不改变 Fast 模型元数据、开关默认值或请求服务等级策略喵~

## Ultra 调查结果

- 原生选择函数先处理模型对应的 Guardian/权限分支，再判断是否已为完全访问、目标是否为 Ultra、当前思考等级是否已经是 Ultra 喵~
- 从非 Ultra 切到 Ultra 且已为原生完全访问时打开独立组合提示；该提示保留取消、保持完全访问与切换受限权限三种结果喵~
- 当前代码没有保存或读取“此前确认过组合提示”的状态，因而不能把重复提示归因为扩展丢失了已有确认记录喵~
- 本轮没有添加全局 JSX 包装、原生回调替换、模拟点击或程序代答确认；“确认一次后记住”未实现，原生权限行为保持不变喵~

## 验证矩阵

- 十一项新增前端行为回归覆盖旧类、新类、响应属性、无响应属性、非 footer 排除、隐藏/断开节点、紧凑布局、重复扫描、重新挂载、显式关闭和扫描入口喵~
- Rust 资源契约验证新选择器、防自插入和 ARIA 状态进入最终嵌入脚本喵~
- 本地独立测试不操作当前应用；完整跨平台 Rust 测试和正式安装包验证由该版本的 GitHub Actions 执行，最终结果以 Release 附带的构建来源为准喵~
